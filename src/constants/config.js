const config = {
  showLeft: true,
  hasCommonBar: false,
  noCommonBar: ['home']
}
const forRouter = {
  router: {
    from: null,
    to: null
  }
}

export default Object.assign({}, config, forRouter)
